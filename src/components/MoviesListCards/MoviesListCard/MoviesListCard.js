import React from 'react';

const MoviesListCard = ({listCard}) => {
    const {page} = listCard;
    return (
        <div>
            <div>{page}</div>



        </div>
    );
};

export default MoviesListCard;