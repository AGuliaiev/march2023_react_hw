import React from 'react';

const MoviesDetails = ({info}) => {
    console.log(info)
    const {} = info;
    return (
        <div>
            MoviesDetails
        </div>
    );
};

export default MoviesDetails;